# Core dependencies - all deterministic libraries
spacy>=3.7.0
sqlglot>=23.0.0
rapidfuzz>=3.0.0
nltk>=3.8.0
networkx>=3.0

# Enhanced NLP libraries (all deterministic)
lemminflect>=0.2.3      # Better lemmatization (95.6% vs spaCy's 84.7%)
word2number>=1.1        # Number word parsing ("twenty" → 20)
dateparser>=1.2.0       # Date/time parsing ("last year" → datetime)
inflect>=7.0.0          # Pluralization/singularization ("countries" → "country")
num2words>=0.5.12       # Ordinal parsing ("third" → 3)

# Coreference resolution (rule-based, configurable)
xrenner>=2.2.0          # Rule-based coreference for multi-turn dialogue

# For evaluation and datasets
pytest>=7.4.0
datasets>=2.14.0        # Hugging Face datasets for easy Spider/SParc/CoSQL loading
